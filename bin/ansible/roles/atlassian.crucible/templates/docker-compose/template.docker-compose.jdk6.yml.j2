services:
  app:
    image: "epicmorg/crucible:{{ item }}"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/crucible:{{ item }}
  app-jdk6:
    image: "epicmorg/crucible:{{ item }}-jdk6"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/crucible:{{ item }}-jdk6
