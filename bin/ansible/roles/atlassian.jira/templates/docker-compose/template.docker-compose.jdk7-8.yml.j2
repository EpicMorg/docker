services:
  app:
    image: "hub.epicm.org/epicmorg/jira:{{ item }}"
    build:
      context: .
  app-jdk7:
    image: "hub.epicm.org/epicmorg/jira:{{ item }}-jdk7"
    build:
      context: .
  app-jdk8:
    image: "hub.epicm.org/epicmorg/jira:{{ item }}-jdk8"
    build:
      context: .
      dockerfile: Dockerfile.jdk8
