services:
  app:
    image: "hub.epicm.org/epicmorg/jira:{{ item }}"
    build:
      context: .
  app-jdk8:
    image: "hub.epicm.org/epicmorg/jira:{{ item }}-jdk8"
    build:
      context: .
  app-jdk11:
    image: "hub.epicm.org/epicmorg/jira:{{ item }}-jdk11"
    build:
      context: .
      dockerfile: Dockerfile.jdk11
