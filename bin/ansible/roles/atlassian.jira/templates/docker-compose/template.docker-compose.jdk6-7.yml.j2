services:
  app:
    image: "epicmorg/jira:{{ item }}"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/jira:{{ item }}
  app-jdk6:
    image: "epicmorg/jira:{{ item }}-jdk6"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/jira:{{ item }}-jdk6
  app-jdk7:
    image: "epicmorg/jira:{{ item }}-jdk7"
    build:
      context: .
      dockerfile: Dockerfile.jdk7
    x-mirrors:
      - quay.io/epicmorg/jira:{{ item }}-jdk7
