services:
  app:
    image: "epicmorg/fisheye-crucible:{{ item }}"
    build:
      context: .
      dockerfile: Dockerfile.jdk7
    x-mirrors:
      - quay.io/epicmorg/fisheye-crucible:{{ item }}
  app-jdk6:
    image: "epicmorg/fisheye-crucible:{{ item }}-jdk6"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/fisheye-crucible:{{ item }}-jdk6
  app-jdk7:
    image: "epicmorg/fisheye-crucible:{{ item }}-jdk7"
    build:
      context: .
      dockerfile: Dockerfile.jdk7
    x-mirrors:
      - quay.io/epicmorg/fisheye-crucible:{{ item }}-jdk7
