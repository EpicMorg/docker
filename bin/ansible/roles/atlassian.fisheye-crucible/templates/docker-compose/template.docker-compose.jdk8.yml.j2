services:
  app:
    image: "quay.io/epicmorg/fisheye-crucible:{{ item }}"
    build:
      context: .
  app-jdk8:
    image: "quay.io/epicmorg/fisheye-crucible:{{ item }}-jdk8"
    build:
      context: .
