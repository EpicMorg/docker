services:
  app:
    image: "quay.io/epicmorg/fisheye-crucible:{{ item }}"
    build:
      context: .
  app-jdk6:
    image: "quay.io/epicmorg/fisheye-crucible:{{ item }}-jdk6"
    build:
      context: .

