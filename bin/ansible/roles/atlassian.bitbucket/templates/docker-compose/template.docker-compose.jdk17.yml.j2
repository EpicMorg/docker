services:
  app:
    image: "hub.epicm.org/epicmorg/bitbucket:{{ item }}"
    build:
      context: .
  app-jdk17:
    image: "hub.epicm.org/epicmorg/bitbucket:{{ item }}-jdk17"
    build:
      context: .
