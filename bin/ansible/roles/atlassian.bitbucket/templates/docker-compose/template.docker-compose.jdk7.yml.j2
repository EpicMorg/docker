services:
  app:
    image: "hub.epicm.org/epicmorg/bitbucket:{{ item }}"
    build:
      context: .
  app-jdk7:
    image: "hub.epicm.org/epicmorg/bitbucket:{{ item }}-jdk7"
    build:
      context: .
