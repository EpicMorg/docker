services:
  app:
    image: "hub.epicm.org/epicmorg/crowd:{{ item }}"
    build:
      context: .
  app-jdk6:
    image: "hub.epicm.org/epicmorg/crowd:{{ item }}-jdk6"
    build:
      context: .
  app-jdk7:
    image: "hub.epicm.org/epicmorg/crowd:{{ item }}-jdk7"
    build:
      context: .
      dockerfile: Dockerfile.jdk7
