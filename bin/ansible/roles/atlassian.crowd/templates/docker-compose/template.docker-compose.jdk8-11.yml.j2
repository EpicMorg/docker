services:
  app:
    image: "epicmorg/crowd:{{ item }}"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/crowd:{{ item }}
  app-jdk8:
    image: "epicmorg/crowd:{{ item }}-jdk8"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/crowd:{{ item }}-jdk8
  app-jdk11:
    image: "epicmorg/crowd:{{ item }}-jdk11"
    build:
      context: .
      dockerfile: Dockerfile.jdk11
    x-mirrors:
      - quay.io/epicmorg/crowd:{{ item }}-jdk11
