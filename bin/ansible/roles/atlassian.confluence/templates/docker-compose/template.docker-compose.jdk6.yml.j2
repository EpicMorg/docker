services:
  app:
    image: "hub.epicm.org/epicmorg/confluence:{{ item }}"
    build:
      context: .
  app-jdk6:
    image: "hub.epicm.org/epicmorg/confluence:{{ item }}-jdk6"
    build:
      context: .
