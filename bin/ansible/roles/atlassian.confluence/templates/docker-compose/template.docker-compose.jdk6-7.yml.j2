services:
  app:
    image: "epicmorg/confluence:{{ item }}"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/confluence:{{ item }}
  app-jdk6:
    image: "epicmorg/confluence:{{ item }}-jdk6"
    build:
      context: .
    x-mirrors:
      - quay.io/epicmorg/confluence:{{ item }}-jdk6
  app-jdk7:
    image: "epicmorg/confluence:{{ item }}-jdk7"
    build:
      context: .
      dockerfile: Dockerfile.jdk7
    x-mirrors:
      - quay.io/epicmorg/confluence:{{ item }}-jdk7
