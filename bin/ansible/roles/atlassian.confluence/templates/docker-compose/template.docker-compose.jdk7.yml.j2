services:
  app:
    image: "hub.epicm.org/epicmorg/confluence:{{ item }}"
    build:
      context: .
  app-jdk7:
    image: "hub.epicm.org/epicmorg/confluence:{{ item }}-jdk7"
    build:
      context: .
