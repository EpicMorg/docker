services:
  app:
    image: "hub.epicm.org/epicmorg/confluence:{{ item }}"
    build:
      context: .
  app-jdk8:
    image: "hub.epicm.org/epicmorg/confluence:{{ item }}-jdk8"
    build:
      context: .
