version: '3'
services:
  app:
    image: "epicmorg/php:php8.1"
    build:
      context: .
  app-nodejs:
    image: "epicmorg/php:php8.1-nodejs"
    build:
      context: .
      dockerfile: ./Dockerfile.nodejs
