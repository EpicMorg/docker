version: '3'
services:
  app:
    image: "epicmorg/php:php7.0"
    build:
      context: .
  app-nodejs:
    image: "epicmorg/php:php7.0-nodejs"
    build:
      context: .
      dockerfile: ./Dockerfile.nodejs
