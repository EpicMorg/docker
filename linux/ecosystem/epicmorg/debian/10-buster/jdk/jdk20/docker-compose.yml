services:
  app:
    image: "hub.epicm.org/epicmorg/debian:buster-jdk20"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:buster-develop-jdk20"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
