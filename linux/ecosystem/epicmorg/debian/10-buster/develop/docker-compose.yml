services:
  app:
    image: "epicmorg/debian:buster-develop"
    build:
      context: .
    x-squash: true
    x-mirrors:
      - quay.io/epicmorg/debian:buster-develop
      - ghcr.io/epicmorg/debian:buster-develop
