services:
  app:
    image: "hub.epicm.org/epicmorg/debian:buster-nodejs8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:buster-develop-nodejs8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
