services:
  app:
    image: "hub.epicm.org/epicmorg/debian:bullseye-jdk20"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:bullseye-develop-jdk20"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
