services:
  app:
    image: "hub.epicm.org/epicmorg/debian:bullseye-nodejs8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:bullseye-develop-nodejs8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
