services:
  app:
    image: "hub.epicm.org/epicmorg/debian:bullseye-dotnet-sts"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:bullseye-develop-dotnet-sts"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
