services:
  app:
    image: "hub.epicm.org/epicmorg/debian:bullseye-dotnet8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:bullseye-develop-dotnet8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
