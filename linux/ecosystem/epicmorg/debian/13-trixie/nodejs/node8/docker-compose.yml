services:
  app:
    image: "hub.epicm.org/epicmorg/debian:trixie-nodejs8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:trixie-develop-nodejs8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
