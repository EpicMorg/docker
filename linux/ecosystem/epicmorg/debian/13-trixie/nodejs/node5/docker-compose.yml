services:
  app:
    image: "hub.epicm.org/epicmorg/debian:trixies5"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:trixie-develop-nodejs5"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
