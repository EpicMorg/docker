services:
  app:
    image: "hub.epicm.org/epicmorg/debian:trixie-jdk20"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:trixie-develop-jdk20"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
