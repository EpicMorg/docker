services:
  app:
    image: "epicmorg/debian:trixie-light"
    build:
      context: .
    x-squash: true
    x-mirrors:
      - quay.io/epicmorg/debian:trixie-light
      - ghcr.io/epicmorg/debian:trixie-light
