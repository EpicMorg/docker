services:
  app:
    image: "hub.epicm.org/epicmorg/debian:trixie-dotnet-sts"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:trixie-develop-dotnet-sts"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
