services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-jdk18"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-jdk18"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
