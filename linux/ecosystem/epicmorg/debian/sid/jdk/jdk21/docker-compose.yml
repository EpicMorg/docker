#version: '3'
services:
  app:
    image: "epicmorg/debian:sid-jdk21"
    build:
      context: .
  app-develop:
    image: "epicmorg/debian:sid-develop-jdk21"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
