services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-jdk20"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-jdk20"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
