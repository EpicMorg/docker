services:
  app:
    image: "epicmorg/debian:sid-jdk24"
    build:
      context: .
  app-develop:
    image: "epicmorg/debian:sid-develop-jdk24"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
