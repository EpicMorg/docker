services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-jdk8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-jdk8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
