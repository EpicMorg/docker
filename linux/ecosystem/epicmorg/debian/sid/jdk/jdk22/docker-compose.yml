services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-jdk22"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-jdk22"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
