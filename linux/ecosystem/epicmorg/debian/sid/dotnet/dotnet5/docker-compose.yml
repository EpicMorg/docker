services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-dotnet5"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-dotnet5"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
