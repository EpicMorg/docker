services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-dotnet7"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-dotnet7"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
