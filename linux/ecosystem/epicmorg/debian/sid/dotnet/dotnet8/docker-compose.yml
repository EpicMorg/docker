services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-dotnet8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-dotnet8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
