services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-dotnet-sts"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-dotnet-sts"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
