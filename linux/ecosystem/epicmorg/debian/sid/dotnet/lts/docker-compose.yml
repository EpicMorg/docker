services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-dotnet-lts"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-dotnet-lts"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
