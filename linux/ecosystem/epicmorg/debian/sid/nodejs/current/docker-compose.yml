services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-nodejs-current"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-nodejs-current"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
