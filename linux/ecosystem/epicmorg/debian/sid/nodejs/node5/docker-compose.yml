services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-nodejs5"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-nodejs5"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
