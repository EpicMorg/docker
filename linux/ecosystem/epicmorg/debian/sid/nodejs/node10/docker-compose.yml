services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-nodejs10"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-nodejs10"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
