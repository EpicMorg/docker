#version: '3'
services:
  app:
    image: "epicmorg/debian:sid-nodejs23"
    build:
      context: .
  app-develop:
    image: "epicmorg/debian:sid-develop-nodejs23"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
