services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-nodejs-lts"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-nodejs-lts"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
