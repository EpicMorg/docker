services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-nodejs20"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-nodejs20"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
