services:
  app:
    image: "hub.epicm.org/epicmorg/debian:sid-nodejs8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:sid-develop-nodejs8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
