#version: '3'
services:
  app:
    image: "epicmorg/debian:jessie-nodejs8"
    build:
      context: .
  app-develop:
    image: "epicmorg/debian:jessie-develop-nodejs8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
