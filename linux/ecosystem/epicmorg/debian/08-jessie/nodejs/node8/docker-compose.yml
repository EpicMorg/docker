services:
  app:
    image: "hub.epicm.org/epicmorg/debian:jessie-nodejs8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:jessie-develop-nodejs8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
