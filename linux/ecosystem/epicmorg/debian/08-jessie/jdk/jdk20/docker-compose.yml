services:
  app:
    image: "hub.epicm.org/epicmorg/debian:jessie-jdk20"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:jessie-develop-jdk20"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
