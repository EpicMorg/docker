services:
  app:
    image: "epicmorg/debian:squeeze-develop"
    build:
      context: .
    x-squash: true
    x-mirrors:
      - quay.io/epicmorg/debian:squeeze-develop
      - ghcr.io/epicmorg/debian:squeeze-develop
