services:
  app:
    image: "hub.epicm.org/epicmorg/debian:squeeze-nodejs8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:squeeze-develop-nodejs8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
