services:
  app:
    image: "hub.epicm.org/epicmorg/debian:stretch-jdk20"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:stretch-develop-jdk20"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
