services:
  app:
    image: "hub.epicm.org/epicmorg/debian:stretch-nodejs5"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:stretch-develop-nodejs5"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
