services:
  app:
    image: "hub.epicm.org/epicmorg/debian:stretch-nodejs8"
    build:
      context: .
  app-develop:
    image: "hub.epicm.org/epicmorg/debian:stretch-develop-nodejs8"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
