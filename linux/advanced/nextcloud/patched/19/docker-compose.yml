services:
  app:
    image: "epicmorg/nextcloud:19-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:19-patched
      - ghcr.io/epicmorg/nextcloud:19-patched
      - docker.io/epicmorg/nextcloud:19-patched
