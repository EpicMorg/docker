services:
  app:
    image: "epicmorg/nextcloud:latest-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:latest-patched
      - ghcr.io/epicmorg/nextcloud:latest-patched
      - docker.io/epicmorg/nextcloud:latest-patched
