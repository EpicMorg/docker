services:
  app:
    image: "epicmorg/nextcloud:22-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:22-patched
      - ghcr.io/epicmorg/nextcloud:22-patched
      - docker.io/epicmorg/nextcloud:22-patched
