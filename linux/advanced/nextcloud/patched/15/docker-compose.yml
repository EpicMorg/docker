services:
  app:
    image: "epicmorg/nextcloud:15-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:15-patched
      - ghcr.io/epicmorg/nextcloud:15-patched
      - docker.io/epicmorg/nextcloud:15-patched
