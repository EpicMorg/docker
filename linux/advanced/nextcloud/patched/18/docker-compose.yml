services:
  app:
    image: "epicmorg/nextcloud:18-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:18-patched
      - ghcr.io/epicmorg/nextcloud:18-patched
      - docker.io/epicmorg/nextcloud:18-patched
