services:
  app:
    image: "epicmorg/nextcloud:20-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:20-patched
      - ghcr.io/epicmorg/nextcloud:20-patched
      - docker.io/epicmorg/nextcloud:20-patched
