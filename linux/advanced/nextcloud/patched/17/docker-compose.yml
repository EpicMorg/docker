services:
  app:
    image: "epicmorg/nextcloud:17-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:17-patched
      - ghcr.io/epicmorg/nextcloud:17-patched
      - docker.io/epicmorg/nextcloud:17-patched
