services:
  app:
    image: "epicmorg/nextcloud:25-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:25-patched
      - ghcr.io/epicmorg/nextcloud:25-patched
      - docker.io/epicmorg/nextcloud:25-patched
