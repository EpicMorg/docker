services:
  app:
    image: "epicmorg/nextcloud:30-patched"
    build:
      context: .
    x-squash: false
    x-mirrors:
      - quay.io/epicmorg/nextcloud:30-patched
      - ghcr.io/epicmorg/nextcloud:30-patched
      - docker.io/epicmorg/nextcloud:30-patched

