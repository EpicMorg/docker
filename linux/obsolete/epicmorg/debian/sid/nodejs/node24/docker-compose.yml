services:
  app:
    image: "epicmorg/debian:sid-nodejs24"
    build:
      context: .
  app-develop:
    image: "epicmorg/debian:sid-develop-nodejs24"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
