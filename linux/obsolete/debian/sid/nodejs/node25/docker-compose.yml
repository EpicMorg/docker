services:
  app:
    image: "epicmorg/debian:sid-nodejs25"
    build:
      context: .
  app-develop:
    image: "epicmorg/debian:sid-develop-nodejs25"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
