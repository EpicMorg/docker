services:
  app:
    image: "epicmorg/debian:sid-nodejs19"
    build:
      context: .
  app-develop:
    image: "epicmorg/debian:sid-develop-nodejs19"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
