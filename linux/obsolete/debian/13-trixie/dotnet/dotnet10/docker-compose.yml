services:
  app:
    image: "epicmorg/debian:trixie-dotnet10"
    build:
      context: .
  app-develop:
    image: "epicmorg/debian:trixie-develop-dotnet10"
    build:
      context: .
      dockerfile: ./Dockerfile.develop
