language: c

services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

jobs:
  include:
    - stage: "EpicMorg production image"
      install: skip
      script: docker build --rm --compress --force-rm -t epicmorg/prod:latest epicmorg/prod/Dockerfile
