language: bash
dist: buster
services:
  - docker 
addons:
  apt:
    packages:
      - docker-ce 
jobs:
  include:
    - stage: EpicMorg production image
      script: docker build --rm --compress --force-rm -t epicmorg/prod:latest -f epicmorg/prod/Dockerfile
