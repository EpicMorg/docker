language: c

services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

jobs:
  include:
    - stage: "EpicMorg production image"
      install: skip
      script: build --rm --compress --force-rm -t epicmorg/prod .
